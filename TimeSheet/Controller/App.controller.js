sap.ui.controller( "sap.ui.project.timeSheet.Controller.App" , {
	/**
	 * View initialization
	 */
	onInit: function() {
		// //	Set compact mode on desktop		
		// if( !jQuery.support.touch ) {
		// 	this.getView().addStyleClass( "sapUiSizeCompact" );
		// }
	}
});